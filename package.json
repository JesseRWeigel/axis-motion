{
  "name": "axis-motion",
  "version": "0.1.0",
  "private": true,
  "description": "Animate OpenType variable font axes from a small timeline DSL, exporting to CSS, WAAPI and video.",
  "license": "MIT",
  "bin": { "axis-motion": "bin/axis-motion.js" },
  "scripts": {
    "test": "node --test test/",
    "verify": "bash scripts/verify.sh"
  },
  "engines": { "node": ">=20" }
}
